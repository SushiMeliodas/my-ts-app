import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
    tableCard: {
      margin: 'auto',
      height: '100%',
      minHeight: '600px',
      maxHeight: '800px',
      width: '100%',
      minWidth: '450px',
      maxWidth: '600px',
      borderRadius: '15px',
    },
  });